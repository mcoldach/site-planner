VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only. NEVER expose to the browser. Used by scripts/ only.
SUPABASE_SERVICE_ROLE_KEY=

# Public ArcGIS REST layer URLs (FeatureServer/MapServer .../<layerId>).
# Consumed by scripts/seed_jurisdictions.ts.
CS_CITYLIMITS_ARCGIS_URL=
EPC_BOUNDARY_ARCGIS_URL=

# Comma-separated parcel numbers for scripts/seed_parcels.ts (Colorado Public Parcels `parcel_id`).
PHASE0_SEED_APNS=
